# Configuration file for TravisCI
os: linux
dist: xenial
language: python
python: "3.6"
notifications:
  email: false

# Only build pushes from the master branch. PRs are always built.
branches:
  only:
    - master

#env:
  #global:
    ## Doctr deploy key for ige-geophysics/ige-geophysics.github.io
    #- secure: "..."

# Setup the build environment
install:
  # Install the requirements
  - pip install -r requirements.txt

script:
  # Build the website HTML
  - make

#deploy:
  ## Push the built HMTL to Github pages
  #- provider: script
    #scipt: doctr deploy . --built-docs _build --deploy-repo ige-geophysics/ige-geophysics.github.io
    #skip_cleanup: true
    #on:
      #branch: master